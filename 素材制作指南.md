# 素材制作指南 —— 用豆包/即梦生成桌宠动作动画和语音包

> 原则：素材你用 AI 生成，程序负责整合。把做好的文件放进对应文件夹即可生效，**不需要改任何代码**。

## 一、动作动画（解决「静态僵硬」问题）

### 放哪里

```
pets/mypet/animation/     ← 新建这个文件夹
├── idle.gif              待机动画（持续循环）
├── click.gif             点击互动动画
├── eat.gif               投喂时吃东西动画
├── sleep.gif             入睡过渡（播一遍后自动接 sleep_loop）
├── sleep_loop.gif        睡着后的循环段（可选）
├── talk.gif              说话时的动画（可选，见下）
└── pet.gif               双击抚摸动画（可选）
```

### 让宠物"说话时嘴动"（talk.gif）

宠物说话（台词、AI 回复、劝休息）时会自动切到 `talk.gif`，说完自动切回 —— 有声音
但嘴巴不动是最出戏的地方，加上它观感提升很大。

生成方式和其他动画一样，用即梦/豆包做一段"在说话"的视频，然后：

```
python scripts/add_asset.py 说话视频.mp4 talk
```

提示词参考：

```
让这个Q版卡通形象一直在说话，嘴巴开合，保持形象和位置不变，
白色纯色背景，动作循环，2秒，卡通风格，画面简洁，不要文字。
```

> ⚠️ **尺寸必须和其它动画一致**（比如都是 320×426）。
> 如果 `talk.gif` 的宽高比和 `idle.gif` 差超过 5%，程序会**主动跳过**说话动画并在气泡里
> 提示你 —— 因为尺寸不一致会让说话瞬间整个窗口跳一下（说完再跳回来），比不动嘴更难看。

没有 `talk.gif` 时说话动画自动不启用，不会报错。

程序会自动优先播放 GIF；没有 GIF 的状态自动回退到 `avatar/` 里的静态 PNG。
**新状态无需写代码**：文件名就是状态名，`apply_state("eat")` 会自动找 `eat.gif` 或 `eat.png`。

### 制作要求

- **尺寸**：160×160 或 320×320（正方形最佳），透明背景
- **格式**：GIF（时长 1~3 秒循环）
- **风格**：和你的照片抠图形象保持一致

### 豆包/即梦提示词模板（图生视频）

先把你的照片抠图成透明 PNG（项目生成器已做过），再喂给即梦/豆包「图生视频」：

```
让这个Q版卡通形象[做动作：开心地咀嚼吃东西/打哈欠伸懒腰睡觉/原地蹦跳打招呼]，
保持形象不变，白色纯色背景（方便后期去背），动作循环，2秒，
卡通风格，画面简洁，不要文字。
```

生成视频后，**直接把它交给程序**就行（向导第④步拖进去，或 `python scripts/add_asset.py 视频.mp4 eat`）：

程序会自动完成这些事，你不需要自己转 GIF：

- **抠背景**：从画面四周采样出背景色，按颜色距离做软 alpha。
  **绿幕、白底、蓝底、任何纯色背景都能用**，不用自己去 ezgif
- **保住同色主体**：白衣服站在白墙前也不会被一起抠掉（只抠与画面边缘连通的区域）
- **抹平硬切**：AI 视频常有"两个镜头硬接在一起"的突变（看起来像抽搐一下），
  会被自动检测出来并用过渡帧摊平；而"轻戳"这类快速动作不会被误伤
- **复杂背景兜底**：背景不是纯色时会自动改用 rembg 逐帧抠（慢一些，但什么背景都能用）

### 两种情况要注意

**① 画面角落有水印**（即梦/豆包默认会加「AI生成」角标）

程序能自动清掉**和主体分开**的那种水印。但如果水印贴在角色身上（比如贴着头发），
就不会去动它 —— 硬识别容易误伤主体（我试过，会把浅色衣服当成水印吃掉）。
这种请**重新导出无水印版本**，或者先把那一角裁掉再生成。

**② 主体和背景同色、而且无缝相连**

比如白猫趴在白床单上、中间完全看不出边界 —— 那种在图像上就是同一块区域，
程序分不出来。换成有色差的背景再生成一次即可。

> 小技巧：即使不做绿幕，纯色背景（白色/浅灰）也完全够用了。

## 二、语音包（解决「语音僵硬」问题）

### 放哪里

```
pets/mypet/sounds/        ← 新建这个文件夹
├── eat.mp3               投喂时的声音（咀嚼/满足感）
├── sleep.mp3             睡觉的呼噜声/轻音乐
├── click.mp3             点击时的反应声
└── pet.mp3               抚摸时的开心声
```

**规则**：文件名 = 状态名。桌宠切换到某状态时会自动播放对应音频，没有就静默跳过，永不报错。
支持 mp3 / wav / ogg。

### 最省事的做法：直接从动画视频里抽音轨

你让 AI 生成的动画视频**通常自带音轨**（哈欠声、咀嚼声、被戳的反应声）。
不用另外去找音效，直接抽出来就行：

```bash
python scripts/extract_audio.py --pet pets/mypet --list   # 先看会抽出什么
python scripts/extract_audio.py --pet pets/mypet          # 抽取并安装
```

脚本会按文件名猜状态（`打哈欠`→sleep、`纯睡觉`→sleep_loop、`进食`→eat、`轻戳`→click），
把音轨统一响度后存成 `sounds/<状态>.mp3`。自动认不出来就用 `--map` 手动指定：

```bash
python scripts/extract_audio.py --pet pets/mypet --map "我的视频.mp4:sleep"
python scripts/extract_audio.py --pet pets/mypet --map "打哈欠.mp4:sleep" --trim 4.5
```

`--trim` 用来把音效裁到和动画一样长，避免"动画演完了声音还在响"（或反过来被截断）。
没有任何音轨内容的视频（比如安静的待机动画）会自动跳过。

### 想自己录/合成语音

豆包 App「语音合成」或「TTS」功能：
- 台词示例：
  - eat：`哇！好好吃！谢谢投喂~`（语气：欢快）
  - sleep：`呼……呼……晚安……`（语气：轻柔）
  - click：`干嘛戳我呀~`
- 生成后下载 mp3，按文件名放进 `sounds/`

### 关于 AI 朗读（PetTTS）

菜单里「语音开关」走的是**实时语音合成**，和上面的语音包是两套机制：

| | 语音包 (sounds/) | 实时合成 (PetTTS) |
|---|---|---|
| 内容 | 你预先做好的固定音频 | 任意文本（台词池、AI 回复） |
| 时机 | 切到该状态时 | 宠物每次说话 |
| 优点 | 音色你完全可控、离线、零延迟 | 什么话都能念 |

默认用 **edge-tts 神经语音**（晓伊等 12 种音色），比系统自带的 SAPI5 老引擎自然得多。
换音色：`python scripts/tts_preview.py --all` 挨个试听，选中后填进 `ai_config.json` 的
`tts_voice`。断网会自动回退系统语音，同一句台词第二次说走本地缓存。

**优先级建议**：
- 状态音效（吃东西、点击）→ 用语音包，真人感最强
- 台词/AI 回复 → 用实时合成，什么都能念

### 生成的动画"抽搐"了一下？

多半是**源视频里有硬切**（AI 图生视频常见：两个镜头被直接接在一起，或动作在中途瞬间跳变），
转换脚本会把这一跳原样保留下来。

判断方法：把生成的 GIF 逐帧看一遍，找出突变的那一帧。脚本本身不做任何插帧或平滑，
所以问题一定在源素材。处理办法：

- 重新生成一段动作连续的源视频（提示词里强调「动作连贯、一镜到底、不要剪辑」）
- 或剪掉硬切前后的片段，只保留连续的那一段
- 或把两段动作分开做成两个状态

## 三、验证

放好素材后重启桌宠：

```
python runtime\main.py --pet pets/mypet
```

- **右键宠物本体** → 🍖 投喂 → 应看到吃东西动画 + 听到声音
- **右键宠物本体** → 睡觉 → 入睡动画播完后会自己接上 `sleep_loop.gif` 循环
- 点一下宠物 → `click.gif` + `click.mp3`
- **右键托盘图标** → 📏 大小 / 👻 鼠标穿透 / 退出
- 无动画的状态自动回退 `avatar/` 里的静态图，无音频的状态静默，不会报错
- 资源包一张形象图都没有时，桌宠会显示「缺图」占位并提示路径，不会静默消失

> 注意：开启了 👻 鼠标穿透后，点击会直接落到下层窗口，**宠物本体就点不到了**，
> 这时请右键**托盘图标**关闭穿透。
