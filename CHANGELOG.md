# PhotoPet 更新日志

## v0.25.0 (2026-09-16)

**语音缓存也加上限了**

- 🧹 **语音缓存上限**（`tts_cache_max_mb`，默认 50MB）：每说过一句**不同**的话就存一个
  mp3，这是整套里**唯一没有上限**的东西 —— 实测跑两天就 2.4MB / 101 个，跑久了会一直涨。
  现在启动时检查一次，超了就删**最久没用过的**（一次多删点，降到上限的 80%，
  免得每说一句都要再清）
- 🔁 **顺带把"最近用过"做成真的**：以前命中缓存时**不会更新访问时间**，
  所以文件时间一直是"创建时间"——照这个删，"久坐提醒"这种天天说、但很久以前第一次
  合成的句子会被当成冷门删掉。现在命中缓存会 touch 一下，删的才是真正冷门的
- 🛡️ 三个边界都验过：设成 `0` = 不限、只删 `.mp3`（同目录其他文件不动）、
  目录不存在不报错
- 删缓存**没有任何副作用**：下次说到同一句，能联网就重新合成，断网就走系统语音
- ✅ 实测：50MB 上限不动、1MB 上限从 2.9MB 清到 0.78MB（留最新的）、
  命中缓存确实 touch（09-15 → 09-16）、PetTTS 启动自动清（3.91MB → 0.78MB）


## v0.24.0 (2026-09-16)

**清理：过期日程会自动清，对话本来就不留**

- 🧹 **新增过期日程清理**：以前日程只进不出 —— 秋招季一天十几场宣讲会，
  `schedule.json` 会一直涨。现在：
  - **开机自动清**：早上打开桌宠时顺手清掉早就过去的一次性日程
  - **菜单手动清**：右键 →「📅 日程 / 待办」→「🧹 清理过期日程（N 条）」，
    条数直接写在菜单上，没有可清的就置灰 —— 一眼知道要不要清
- ⚖️ **两条边界想清楚才动的手**（不然会删掉不该删的）：
  - **重复日程一条都不清** —— `每天/每周/每月/每年` 永远有下一次，
    而且下次时间是从原始起点算出来的，**删了等于把整条重复规则也删了**
  - **最近过去的留着**（默认 7 天，`schedule_keep_past_days` 可调）：
    用户可能想回看"上周去了哪些宣讲会"。设成 0 就只手动清
- 🧠 **顺带加了个「它记住了什么」**：直接打开记忆文件 `MEMORY.md` 让你自己看/改/删 ——
  比再做一个删除按钮实在
- 💬 **关于"对话怎么清"**：查了一遍，**对话压根不落盘** —— `AIChat.history` 只是个内存列表，
  关掉桌宠就没了，用户数据目录里只有 `schedule.json` / `tasks.json` / `MEMORY.md` /
  `weather.json`，**没有任何聊天记录文件**。这是刻意的（隐私）。
  窗口里的按钮改叫「**清空对话**」，并说清它清的是"上下文"而不是"记忆"：
  `对话清掉了…（它记在心里的那些事还在，要忘掉得跟它说「忘掉 XX」）`
  —— 这两件事很多人会混
- ✅ 实测：清理边界（清掉 30 天前/10 天前的，留住 2 天前的、今天明天的、以及所有重复日程）、
  开机自动清理、菜单条数与置灰状态


## v0.23.0 (2026-09-16)

**能查宣讲会的详细信息了**

- 📋 **抓宣讲会时把详情页链接一起存下来**：以前只存了公司名/时间/地点，**链接被丢掉了**，
  所以想查"这场招什么岗位"没法查。现在日程里带着链接，聊天里直接问：
  `帮我查一下吉安生益那场宣讲会的详细信息`
- 🔍 **新增 `read_event` 工具**：它找到那条日程、去读详情页、挑重点回答。实测答出来的是
  「上市公司生益电子的高端印制电路板基地，做服务器和汽车电子，规模1000-5000人」
  「五险一金、年终双薪+年度分红、公寓式宿舍拎包入住、伙食补贴」
  - **而且它诚实地说明了看不到的部分**：详情页里简历邮箱是打码的
    （`sh******************`），它就照实说"要登录才能看"，没有编一个邮箱出来
- 📎 `list_events` 的输出也带上了详情页链接，所以"今天都有哪些宣讲会"这类问题
  它会顺带把链接给出来
- 🧩 `read_event` 是一次调用搞定（找到日程 + 读页面），不用模型先查列表再读链接转两圈

- 🔧 **给已有的日程补链接**：你之前抓进去的那批日程是旧版本存的、**没有链接**，
  而重复抓取会被去重逻辑跳过 —— 等于它们永远查不了详情。现在重复抓取时会把新拿到的
  链接**补给已有条目**（实测 6 条老日程全补上，40/40 都带链接了），不用先删再导
- 🐛 **顺手修了一个影响面更大的 bug**：模糊匹配的门槛是"最长公共子串 ≥ 2 个字"，
  而中文里「**公司**」这两个字几乎每个标题都有 —— 于是拿「不存在的公司」去查，
  它会回一句"**有 32 条都对得上**"（实测）。这个 bug 同时影响删日程、改待办、查详情。
  改成**相对阈值**（公共子串要占较短那方的一半）：
  - `吉安生益` vs `吉安生益电子有限公司` ✓ 命中
  - `开会的提醒` vs `开会` ✓ 命中（模型爱用这种描述式说法）
  - `不存在的公司` vs 各个公司名 ✗ 挡掉，改成回"没找到，现有的是 A、B、C"
    ——**让模型换个准确标题再试**，比匹配上一堆不相干的安全
- ✅ 实测：8 组匹配用例 + 真实对话验证两种问法（查某场详情、问今天有哪些场）；
  既有工具测试全部回归通过


## v0.22.0 (2026-09-16)

**下雨提醒（该收衣服了）**

- ☔ **新增下雨提醒**：未来 N 小时内要下雨就提醒你收衣服/带伞。实测说出来的是
  `大概 2 小时后可能有雨（53%），阳台上的东西收一下？`
- 🌤️ **数据源 Open-Meteo**：免费、**不用注册、不用填 key**。对比过几个 ——
  和风天气要注册拿 key，中国天气网没有官方接口只能爬页面（不稳），wttr.in 字段太少。
  Open-Meteo 逐小时预报里直接有降水概率和降水量，对"要下雨了吗"刚好够用
- 🤫 **不做每日天气播报** —— 这又是"有事要办"那条边界的一个实例：
  **下雨要收衣服是"事"，"今天 25 度晴"不是**。用户反感的是没话找话，不是有事说事
- 🎯 **两个条件同时满足才提醒**：降水概率够 **且** 降水量够。只看概率的话，
  "60% 概率 0.0mm"也会报（那是"可能下但没量"）—— 把没下雨的天气喊去收衣服，
  几次之后这个提醒就被无视了，提醒的价值在于准
- 🔁 **同一场雨只说一次**：记下"提醒到的是哪一小时"，所以每 30 分钟查一次也不会反复喊，
  **重启桌宠也不会重喊**（状态存在 `weather.json`）
- 📁 **菜单整理**：主菜单从 **21 项降到 16 项** —— 日程/待办/导入/秋招/天气这些
  收进了「📅 日程 / 待办」子菜单。之前新功能加到找不着，是时候归拢了
- ✅ 实测：18 个用例（概率不够不报、有概率没雨量不报、雨在窗口外不报、马上要下、
  去重、重启不重喊、没配城市安静、城市查不到安静、状态文件损坏不崩）；
  真实接口验证 —— 哈尔滨（今天有雨）会提醒、南昌（三天无雨）保持安静

- 🐛 顺手修一个潜在 bug：天气监视器缓存的判断用了 `hasattr`，
  属性存在但为 None 时会直接返回 None（改成 `is None` 判断）


## v0.21.0 (2026-09-16)

**它能读网页了（免费联网）**

- 🌐 **新增「读网页」工具**：聊天里发个链接、或者说「帮我看看这个页面」「这个链接讲了什么」，
  它把页面正文读进来再回答。**零额外成本** —— 走现有对话接口，不额外收搜索费
- 🔒 **加了 SSRF 防护**（这是接这个功能的前提）：工具的地址是**大模型给的**，不可信 ——
  它可能（被提示词带偏、或被诱导）去读 `http://192.168.1.1/`、`http://localhost:8000/`
  这类**内网地址**，那等于把你家路由器、公司内网暴露给外部模型去试探。
  所以每次抓取前先解析域名、确认**所有**解析结果的 IP 都是公网地址才放行
  （只查第一个 IP 的话，一个域名同时解析到公网和内网就能绕过去）。
  实测挡住：localhost / 127.0.0.1 / 192.168.x / 10.x / 169.254.169.254（云元数据地址）/ file://
- 🧩 顺手把"抓网页"抽成公用模块 `pet_web.py`，就业网抓取和读网页共用一份实现

- 🐛 **搬代码时踩的一个坑，值得记**：那个 CMS 的正文是压缩藏在 script 里的，
  解法注释里写着 `zlib解压 → 去掉前 77 字符 → base64解码 → 去掉前 40 字符` ——
  我照着搬，结果**读出来是乱码**。原因是**那两个数字不是固定的**，它对应"前缀标记的长度"，
  而服务端不同变体给的前缀长度不一样。改成**用正则找真正的 base64 段、解码后从第一个 `<` 切**，
  从此不依赖任何魔数
- ✅ 实测：SSRF 八种地址逐个验证（内网/本机/元数据地址/file 全部拒绝，公网放行）；
  用存下来的真实页面离线验证解码（列表页解出 12551 字符/20 条，"暂无数据"变体解出 214 字符/0 条）；
  在线抓取 40 条；`read_url` 读同一个就业网列表页从 **313 字符的页面框架**变成
  **1174 字符的真实宣讲会内容**


## v0.20.0 (2026-09-16)

**换学校也能用：网址可填，认不出来的站自动交给大模型**

- ⚙️ **新增「秋招设置」窗口**（右键 →「⚙ 秋招设置（换学校 / 改网址）」）：
  填就业网地址、只看未来几天、自动同步间隔。以前这些只是 `ai_config.json` 里的
  数字，**用户根本不知道有这个设置**
- 🔍 **带「测试这个地址」**：填完直接告诉你结果，别等抓完才发现填错了
  - `✓ 能读！内置规则解析出 20 场宣讲会，分布在 2 天里…`
  - `△ 这页看着是宣讲会页面，但内置规则认不出它的格式 —— 抓取时会自动交给大模型`
  - `✗ 这页看起来不是宣讲会/招聘会列表页，换个更具体的地址`
- 🧠 **认不出来的网站自动改走大模型**：不同学校可能用的根本不是同一套 CMS。
  内置规则只认这一套（快、免费、离线），认不出来就把**页面正文**交给大模型抽 ——
  和"导入文件"共用同一条链路。**换学校、换系统都能用**
- ⚖️ 顺带立了条规矩：**日期过滤必须由代码做，不能交给模型判断**。

- 🐛 **三个实测抓出来的问题**：
  - 对"不是这套 CMS"的地址，`fetch_teachins` 会**重试 6 次**（每次撞 20 秒超时），
    用户干等 **200 秒**。现在一发现"页面能打开但里面没有这套系统的数据"就立刻停，
    200 秒 → 20 秒
  - 修完还是慢：`break` 只跳出了重试循环、**外层翻页循环又抓了一遍**同样的慢页面，
    多花 17 秒（这处漏掉很隐蔽，因为功能是对的、只是慢）
  - 让大模型从网页抽事件时，它会**自作主张判断"这条已经办过了"然后漏掉** ——
    实测同一页面两次调用：一次 15 条、一次 **0 条**。改成提示词明确说"不许自己筛，
    过期的由程序过滤"，连续两次都稳定 16 条
- ✅ 实测：设置窗口能建、当前地址测出"✓ 能读 20 场"；换成非 CMS 地址后
  内置规则秒退 → 抓页面 → 大模型 7 秒抽出 16 条


## v0.19.1 (2026-09-16)

**修：上一版的秋招入口根本没进菜单 + 自动同步补上开关**

- 🐛 **「🎓 抓宣讲会」在菜单里不存在**：v0.19.0 说做了，其实那次批量插入**没生效**。
  更糟的是我的检查方式有毛病 —— 我用「文件里有没有'抓宣讲会'这个字符串」来验证，
  结果命中的是**函数注释**（`main.py:1233` 的文档字符串），菜单那行压根没插进去却显示"通过"。
  这类"用自己的注释把自己骗过去"的假验证，比没测还危险。现在改成**直接把菜单 dump 出来
  逐项核对**
- ✨ **补上「⏱ 宣讲会自动同步」开关**：自动抓取以前只是配置里一个数字
  （`jobfair_auto_hours`），**没有界面** —— 用户想开自动抓取只能去手改 json。
  现在是个可勾选项：勾上=每 6 小时自动抓一次，勾掉=只手动抓
- 📌 **托盘菜单也加了入口**：宠物右键菜单已经排到 20 项，新功能很容易翻不到 ——
  右下角托盘图标右键也能抓宣讲会
- ✅ 实测：dump 两份菜单逐项核对；开关关→配置写 0 且定时器停、开→写 6 且间隔 6 小时、
  菜单勾选状态实时跟上


## v0.19.0 (2026-09-16)

**秋招模式：自动抓宣讲会**

- 🎓 **右键「🎓 抓宣讲会」**，它去就业网把最近的宣讲会捞回来，弹预览让你勾选，
  确认后进日程。抓到的信息很全：**公司名 / 日期 / 开始结束时间 / 地点 / 线上还是线下**
  （地点写进日程备注，比如"大学生活动中心318 13:30-15:30"）
- ⏰ **提醒提前 30 分钟**（普通日程是 15 分钟）：宣讲会要提前到场占座、还要找教室，
  15 分钟常常不够
- 🌐 **数据源有点绕，说明一下**：就业网的列表**不是普通 HTML**，而是 CMS 把内容
  base64 + zlib 压缩后塞在页面的 `<script>` 里（国内 CMS 常见的 "jz" 混淆）。解法是
  `zlib 解压 → 去掉前 77 字符 → base64 解码 → 去掉前 40 字符 → 真正的列表 HTML`。
  用 Python 标准库就能做，**没加任何新依赖**
- 🔁 **源不稳定，所以带重试**：实测同一地址连抓 4 次的结果是 20/20/0/0 条，像是多台
  后端节点缓存不一致。第一页最多重试 6 次，**抓不到会如实说"没抓到"，绝不假装
  "今天没有宣讲会"** —— 秋招里悄悄漏掉一场比报错严重得多
- 🎯 **不过滤原则（用户明确要求）**：除了"日期在今天之后"这一个安全过滤，
  **不做任何筛选**。宁可多报几条不相关的，也不能因为"看着不像目标行业"就丢掉一场 ——
  漏掉的机会补不回来，多看的几秒不值钱。要筛由用户自己加关键词（默认不筛）
- 🔄 **定时自动同步**（`jobfair_auto_hours`，默认关）：开了之后每隔几小时自动抓一次，
  新发现的**直接加进日程不打扰**。这是全项目唯一一处"未经确认就写日程"的地方，
  所以三条限制卡死：只加未来 N 天内的、只加就业网上的公开信息、开关默认关
- 📐 和 v0.18 的**同时段并排显示**正好配成一套 —— 实测今天 11 场宣讲会里有 3 个时段
  撞车（10:00 两场、13:30 三场、16:00 三场），打开日程卡一眼看到"同时 3 场，得挑一个去"
- 🏫 数据源**可配置**（`jobfair_url`）：这个站是「江西省大学生就业服务平台」的学校子站，
  同一套 CMS 覆盖多所高校，换学校只要改配置里的地址

- 🐛 **两个实测抓出来的问题**：
  - `to_events(remind_before=30)` 的提前量**没进事件字典**，下游拿不到、全都退回 15 分钟，
    和注释里写的完全不符（端到端测试抓到的）
  - 第二页请求常常挂到 20 秒超时 × 2 次，**用户要干等 40 秒**才看到预览 ——
    第一页已经有 20 条了，为多拿一页等这么久不值，改成后续页 8 秒超时
- ✅ 实测：真实抓取 20~40 场、预览默认全选、重复抓取 0 条被勾（不会重复添加）、
  全部是今天及以后、提前量 30 分钟生效；并为「线下不标注、线上才标」做了取舍
  （每条都挂"（线下）"会把标题撑得老长）


## v0.18.0 (2026-09-16)

**同时段的安排并排显示（秋招宣讲会那种场景）**

- 📐 **同一时刻的几场并成一行**：秋招季常见"同一时间两场宣讲会"。以前会排成两行，
  看起来像"先开一场再开一场"—— 会让人误判自己两个都能去。现在并成一条：

  ```
  今天  13:22  华为宣讲会、腾讯宣讲会、字节宣讲会
               同时 3 场，得挑一个去
               还有 1 小时 59 分 · 提前 30 分钟提醒
  ```

- 🗣️ **提醒也一句话说完**：`20 分钟后开始，有 3 场撞在一起了：华为宣讲会、腾讯宣讲会、字节宣讲会`
  —— 分别说三句听起来像有三件事要办，其实是一件事：你得马上做取舍
- 🔢 同一时刻超过 3 场时缩写成「A、B 等 5 场」，卡片不会撑爆
- 🧩 分组逻辑放在 `pet_schedule.group_by_time` / `titles_text`（纯函数，可单测），
  卡片和提醒共用同一份 —— 免得两处对"同时"的判断不一致
- ✅ 实测：新增 8 个用例（分组、标题拼接、超量省略、不改排序），并渲染出卡片核对


## v0.17.0 (2026-09-16)

**把 Excel/Word 直接丢给它，自动变成日程**

- 📥 **导入日程文件**：把 `.xlsx` / `.xls` / `.docx` / `.doc` / `.csv` / `.txt`
  **直接拖到宠物身上**，或者右键「📥 导入日程文件…」。它会读出里面的安排，变成日程提醒
- 🛣️ **两条路并联，能省则省**：
  - **规整的表格走规则解析** —— 有表头（日期/时间/事项/重复/备注）的直接按列取。
    不花钱、不联网、毫秒级，绝大多数人做的日程表就是这种
  - **规则啃不动的交给大模型** —— "下周三下午三点开会"这类自然语言、或者 Word 正文里
    混着的安排。**复用已有的工具调用链路**：模型看到的还是平时那个 `add_event`，
    只是写入目标换成了临时文件，读完就删 —— 所以日期换算、参数校验、错误兜底
    全都是同一套代码，没有为导入另写一套规则
- 👀 **先给清单过目，确认了才写**：这一步是刻意加的，不是偷懒。一个 Excel 几十行，
  直接灌进去、里面几条解析错了，用户根本发现不了，等哪天提醒没响才回头查 ——
  代价太大。预览里可以逐条取消，**日程里已有的条目会标出来且默认不勾**
- 🔁 **去重**：同一个文件导两次不会变成两份（比的是"标题 + 解析后的时间"，
  所以文件里写 `2026/9/20`、日程里存 `2026-09-20` 也认得出是同一天）
- 📄 读文件**不需要任何新依赖**（openpyxl / python-docx / xlrd 环境里都有）；
  老的 `.doc` / `.xls` 走本机 Office，没装会明确提示另存为新格式，而不是丢个异常出来
- ⚠️ 顺带说清一件事：**文件内容会发给大模型**（走第二条路时）。所以文档里也写了
  别拿论文、申请表这类东西来导

- 🐛 **两个只有测试才抓得出来的 bug**：
  - 表头检测时只登记了"日期/时间/事项"三列，**"重复"和"备注"两列被静默丢掉** ——
    明明写了"每天"，导进来全变成"只一次"
  - CSV 是按**逗号**分隔的，我却只按 `|` 和空格切，整行成了一个格子、全部跳过
- ✅ 实测：`pet_import` 45 个用例（各种日期时间写法归一、该放弃时放弃、脏数据）；
  端到端跑通四条路 —— 表格解析导入 4 条、重复导入 0 条被勾、自然语言解析 3 条
  （「下周三下午三点开会」正确算成 2026-09-23 15:00）、坏格式给明确提示


## v0.16.0 (2026-09-16)

**待办 —— 从"会用工具的聊天机器人"往"agent"走的第一步**

- 📋 **新增待办（任务层）**：这是这次最要紧的改动。以前它只能记**事实**（"你对花生过敏"）
  和**日程**（"三点开会"），记不住"**我在推进一件事**"。现在你说「我要准备下周的答辩，
  得整理大纲、做 PPT、还得演练一遍」，它记成的是一条**待办**，自动拆出 3 个子步骤，
  之后你说「大纲整理完了」它会把那一步打勾、状态转成"进行中"
- 📅 **待办和日程是两件事，刻意分开存**：
  - 日程回答"**什么时候**要做什么"——有确定时间点，到点提醒
  - 待办回答"**有什么事还没做完**"——有状态、有进度，可能压根没有截止时间
  - 一条待办可以挂 `due`（期望做完的时间），但"准备答辩"这种说不出是几点的会，
    以前根本没有地方放
- ⏰ **主动开口的口径改了，这是产品方向的调整**：从"按时间"改成"**按事由**"。
  待办**逾期**或**今天到期**时开口（`「上周就该交的材料」已经逾期 2 天了，还做吗？`），
  同一条一天只说一次。**定时闲聊和早晚简报依然不做** —— 之前的边界是"只在互动和
  久坐提醒时开口"，现在加上第三条"有事要办"。反感的是没话找话，不是有事说事
- 📋 **待办卡**：右键「📋 待办」一眼看到哪件事要炸了（逾期排最前，时钟/圆点区分紧急度）
- 🧰 工具箱从 6 个加到 **9 个**：新增 `add_task` / `list_tasks` / `update_task`
- 🧹 顺手把重复的模糊匹配逻辑抽成 `pet_text.best_match`（`ai_tools` 里那份私有实现删掉了）——
  理由见 v0.14.0 那个"模型说删了其实没删"的教训，这套匹配现在被日程和待办共用

- ✅ 实测：`pet_task` 39 个用例（状态流转、子步骤打勾、逾期判定、注入上限、存档往返、
  脏数据）全过；再用**真实 DeepSeek** 跑通「记待办 → 查待办 → 打勾某一步」，
  并核对自动开口的触发与去重


## v0.15.0 (2026-09-16)

**它开始记得你了**

- 🧠 **长期记忆**：跟它说「记住：我对花生过敏，点菜要避开」，之后**新开一次对话**
  再问「我点菜要注意什么」，它照样答得上来。记忆就存在 `MEMORY.md` 里，
  一个纯文本文件，你随时能打开看、能改、能删
  - 做法是**文件式记忆**（参考 PetGPT 的 workspace 方案）：每轮把内容拼进 system prompt，
    由模型自己维护。**没上向量库** —— 桌宠这点记忆量（几千字）全文本注入准确率 100%、
    零依赖零 embedding 成本，向量检索在这个量级是杀鸡用牛刀
  - 文件名**由代码写死**，模型只能提供"记什么"、不能指定路径 —— 所以不存在写到别处去的
    风险。这是刻意的：不做通用的文件写工具
  - **敏感信息拦两道**：密码、银行卡、身份证、验证码这类内容，提示词里写了不许记，
    代码里也真的拦下来（命中就拒绝并提醒你别把这些交给它保存）
  - 注入有上限（3000 字），超了只带最近的部分，并注明"更早的 N 条没放进来"；
    同一条内容不会重复记；一句闲聊撑不爆文件（单条超过 200 字会截断）
- 💬 **它可以说点自己的话了**（`ai_chatter_seconds`，**默认关闭**）：开了之后它会拿
  "现在几点、你忙了多久、下一个安排是什么、它自己饿不饿"这些上下文**现想一句**，
  而不是念固定台词。实测生成的是「开会在即，我也饿扁了~」——
  用上了上下文，不是干巴巴的废话
  - 三道闸门：**默认关**（这是持续烧 token 的功能，而且"时不时自己说一句"正是
    安静陪伴要避免的）、**一天最多 20 次**（可配）、**睡着/全屏/正在说话时不插嘴**
  - 主动搭话**不进对话历史** —— 否则它自己嘟囔的几句会污染你的上下文，
    你再问它"刚才说啥"它会答非所问
- 🖼️ **修掉对话框左上角没有图标**：程序从来没给应用设过窗口图标（托盘图标是单独设的，
  不会自动带到普通窗口上），所以对话窗、设置窗、添加日程窗的标题栏左上角一直是
  Qt 的空白默认图标。改成应用级设置，所有窗口和任务栏一起生效

- ✅ 实测：记忆 26 个用例（去重、敏感信息拦截、超长截断、注入上限、忘掉、重启后仍在）
  全过；再用**真实 DeepSeek** 跑通跨会话验证（会话 A 说过敏 → 清空上下文 → 会话 B
  仍然答得出来）。顺手让它全程用中文（之前偶尔夹英文）


## v0.14.0 (2026-09-16)

**它会做事了 —— 工具调用**

- 🛠️ **接上工具调用**：以前它只会说，现在能动手。聊天里说「明天下午三点提醒我开会」，
  它自己换算成绝对日期、调 `add_event` 记进日程，然后回你一句「记好啦」。
  目前开放四个工具：

  | 工具 | 你这么说 | 它做什么 |
  |---|---|---|
  | `add_event` | "明天下午三点提醒我开会" | 记一条日程（支持每天/每周/每月/每年重复） |
  | `list_events` | "我接下来有什么安排？" | 查日程再用人话答你 |
  | `delete_event` | "把开会的提醒取消掉" | 按标题删掉那条 |
  | `pet_action` | "喂你吃东西" / "去睡觉" | 投喂 / 睡觉 / 醒来 / 摸摸 |

- 🔒 **安全边界是刻意画死的**：只开放**日程**和**宠物自己的动作**，不做文件读写、不执行
  命令。调研里 PetGPT 那套 read/write/edit 文件工具确实强，但一旦模型能写文件，路径沙箱、
  用户确认、误删防护就全都得跟上 —— 桌宠并不需要这些能力，不开就没有这些风险
- 📅 **把"今天几号"喂给模型**：system prompt 里带上当前日期和星期，否则它算不对
  "明天""下周三"。实测「明天下午三点提醒我开会」被正确记成 `2026-09-17 15:00`
- 🧯 **工具出错不中断对话**：参数不是合法 JSON、日期看不懂、参数名写错、工具不存在 ——
  全都转成一句**给模型看的错误说明**让它自己改，而不是抛异常把整轮对话打断

- 🐛 **一个最阴的失败被端到端测出来了**：模型嘴上说"开会的提醒已经取消啦"，日程里那条
  **其实还在**。查下来是它拿「开会的提醒」这个**描述**去指代标题只有「开会」的那条，
  精确包含匹配没命中，返回了错误 —— **而模型无视了错误结果照样报成功**。
  用户会以为删了，实际没删，这是最坏的一种失败。修法两条：
  - 匹配改成"先精确包含，再按**最长公共子串**模糊找"（两个字以上才算数）
  - 工具调用加一行 stderr 日志 —— 模型到底调没调、调成什么样，出问题时这是唯一线索
- ✅ 实测：`ai_tools` 24 个用例（模糊匹配、一条命中多条要拦下来、参数写歪、非法 repeat…）
  全过；再用**真实 DeepSeek** 跑通"记/查/删/喂食"四个场景，并核对记下的时间；
  `pet_schedule` 61 个用例、档案 60+ 用例、农历 73,019 天逐日比对同步回归


## v0.13.0 (2026-09-16)

**聊天窗口 + 想出一句说一句 + 一个能填 key 的地方**

- 💬 **真正的对话窗口**：原来右键「和它聊天」是弹个输入框一问一答，问完就没了 ——
  看不到上下文，也没法连着聊。现在是一个记忆里那样的窗口：你说的话靠右、宠物说的靠左，
  回车发送、Shift+回车换行，可以连着聊、可以清空
- ⚡ **流式输出**：回复是**一个字一个字冒出来**的，不用干等整段生成完
- 🗣️ **想出一句说一句**：把流式文本按**句末标点**切成完整句子，**凑够一句就先读出来**，
  而不是等整段生成完再开口 —— 实测同一句回复，改之前要等全部生成完（约 2 秒）才出声，
  改之后第一句一出来就开口
  - 逗号**不算**断句（"你好，我是小王"切成两句会很怪），但一句话超过 32 字会按逗号兜底切开，
    否则一句长话又要等很久
- ⚙️ **API 设置窗口**：以前填 key 只能去手改 `ai_config.json` —— 而且没配 key 时
  「和它聊天」这一项**是隐藏的**，新用户根本找不到入口。现在这个入口一直都在，
  点开就能填 key / 接口地址 / 模型名，还能**一键测试连接**（直接告诉你连没连上、
  模型回了什么）。key 以密码形式显示，带「显示」开关
  - 目前默认 DeepSeek，但接口地址和模型名都能改，任何 OpenAI 兼容服务都能用
  - 保存时**只覆盖改动的那几项**，不会把音色、提醒周期这些配置冲掉
- 🔊 **语音改成排队播放**：TTS 原来是"新句子直接 `setSource` 掐掉上一句"，
  单句对话看不出来，但流式朗读会把每句都截断。现在答完一句自动接下一句，
  口型也不会中途收回

- ✅ 实测：句子切分器 6 组用例（逗号不断句 / 一口气三句 / 超长兜底 / 只有残句 / 换行）；
  再用**真实 DeepSeek 接口**跑通端到端 —— 流式 32 次增量更新、切出 2 句自然话、
  全文正确落到窗口；设置窗口的「测试连接」也真连了一次


## v0.12.0 (2026-09-16)

**日程 —— 让它记得你接下来要干什么**

- 📅 **新增日程**：右键宠物 →「📅 日程」看下一个安排；「➕ 添加日程…」填个小表单加一条
  （日期时间直接手写：`2026-09-16` 或只写 `09-16`，时间和它说 `14:30`）。日程卡用的
  是**同一张纸**，和档案卡一致
- ⏰ **提前量提醒，不是到点才叫**：默认提前 15 分钟开口（每个事件可自己设，填 0 就是
  到点才说）。提醒的那句话会带上还剩多久：`12 分钟后有安排：交房租`。这是全项目里
  **唯一**允许日程主动说话的时机 —— 不播报"今天有几件事"，只在该动身之前说那一次
- 🔁 **重复**：每天 / 每周 / 每月 / 每年。两个边界都按"人话"处理：
  - 「每月 31 号」遇到 2 月**顺延到月末**（28/29），而不是跳过整个月 ——
    跳过的话"每月最后一天交房租"就没法表达了（这和 `dateutil.rrule` 的默认行为相反）
  - 起点在**未来**的事件不会被提前拉到本月（`10-31` 开始的每月事件，下次就是 `10-31`）
- 💤 **关机期间错过的安排，开机汇总成一条**说一次（`你不在的时候错过了 3 个安排：开会、交房租`），
  **不逐个重放** —— 那是轰炸，不是提醒（这个设计参考了 Zoria-Lind/desktop-pet-reminder）
- 📄 **存档是可读的 json**：`schedule.json` 摆在用户数据目录里，字段就是
  `title / date / time / repeat / remind_before_minutes`，想手改直接改，也能用别的程序生成
- 🧵 **日程是"人的事"不是"某只宠物的事"**：存在用户级目录、全局共用一份，所以同时养两只
  宠物也不会各提醒一遍（提醒去重天然生效）
- 🧱 顺手把纸张绘制抽成了 `PaperCard` 基类，档案卡和日程卡共用一份画纸的代码
- ✅ 实测：`pet_schedule.py` 61 个用例（含月末顺延、闰年 2/29、跨年、未来起点、
  提醒窗口边界、脏存档）全过；再用真实 `PetWindow` 走了右键菜单 → 卡片弹出

- 🐛 **三个被单测抓出来的 bug**（都不是肉眼能发现的）：
  - 提醒窗口写成 `now < when`，导致**正好在整点那一分钟启动时永远不提醒**
  - `save()` 每次都会刷新 `last_seen`，而它是"上次退出时间" —— 被抓到后开机补报
    就永远查不出东西（改成只在退出时更新）
  - 每月/每年的重复少了"起点还没到就照原样返回"，未来的事件会被提前拉到本月


## v0.11.0 (2026-09-15)

**人物档案 —— 让它从"一只宠物"变成"一个人"**

- 📄 **档案卡做成了"一页纸"**：暖纸色 + 淡蓝横线 + 左侧红边线 + 楷体，像从横线本上
  撕下来的一页。纸的质感是三样叠出来的 —— 纸底斜向微渐变、噪点平铺（纤维感，程序生成，
  不需要贴图文件）、软阴影（没有阴影纸就"贴"在屏幕上而不是"浮"着）。

  - **横线对齐到字行**：第一版按固定间距平铺，文字骑在两条线中间，一眼就是假的。
    现在从正文的实际行高（`QFontMetrics.lineSpacing()`）和首行位置反推线位，字才是
    坐在线上的；行间还空一整行，更像手写
  - 行首图标是**手绘单色简笔**（生日★ / 相识🗓 / 陪伴🕐 / 相恋♥），不再用彩色 emoji ——
    有立体光影的 3D 图形压在纸面上会跳戏。形状都极简是有原因的：**实测过 13~15px 下的
    辨识度**，蛋糕、礼物、蜡烛这些细节多的画出来全糊成一团，只有实心剪影和"一圈两根针"
    认得出来
  - 空档案时给的是填写指引；指引是 11px 小字，所以**不参与横线对齐**（拿正文行高去套它，
    字和线会交错着压在一起）

- 📇 **新增人物档案**：右键宠物 →「📇 档案」，一张小卡片告诉你它是谁、陪了多久、
  下一个纪念日还有几天。字段写在 `pet.json` 的 `profile` 块里：

  ```json
  "profile": {
    "title": "你的专属桌宠",
    "birthday": "03-14",
    "meet_date": "2026-09-14",
    "love_date": "2026-09-20",
    "intro": "从一张照片里长出来的小家伙。"
  }
  ```

  - `birthday` / `meet_date` / `love_date` **每年重复**算倒计时；当天显示
    `🎉 就是今天`（是 0 天，不是 365），带年份才会额外显示"26 岁""第 N 天"
  - 只写 `MM-DD` 也认——能算倒计时，但算不出"第几天"（没年份就没有起点）
  - **`meet_date` 不填就自动用第一次打开它那天**，所以每只桌宠都有"陪伴第 N 天"，
    不用先填档案才看得见
  - 2/29 在平年按 2/28 过；日期写成 `2026/9/14`、`2026.9.14` 都认；
    整块写坏了（比如 `"profile": "随便写的"`）只当没填，不会让桌宠起不来
- 🔒 **没有占用 `persona`**：`persona` 是喂给 AI 对话的 system prompt 字符串
  （`pet_ai.py:117`），把它改成结构化对象会让 AI 对话直接出错，所以档案单独放
  `profile` 块
- 🤫 **严格遵守"安静陪伴"**：档案卡只在**你点开时**出现，点一下就收起（再点菜单
  也能收起），不会自己弹、不会说话、不碰任何养成数值 —— 它是查询，不是提醒
- 🌙 **支持农历生日**：有人过公历生日，有人过农历。加 `"birthday_calendar": "lunar"`
  即可，卡片会写成三行：

  ```
  农历十月十九 · 今年 11-27
  24 岁 · 还有 73 天
  公历 2001-12-03 出生
  ```

  - **第三行是必须的**：第一版只显示「农历十月十九 · 公历 11-27」，结果用户看到
    自己填的是公历 12-03、卡上却是 11-27，以为程序把公历生日算错了。换算没错
    （12-03 正是农历十月十九），错在**卡上从头到尾没出现过用户填的那个日期** ——
    加上「今年」二字和出生日期之后就不会误会了

  - 农历生日对应的公历日**每年都不一样**（农历十月十九 = 2025-12-08 / 2026-11-27 /
    2027-11-16），所以必须查表换算，不能拿公历月日去减
  - 落在**明年**时会补上年份（`公历 2027-11-16`）—— 生日刚过的第二天就会遇到这种情况，
    只写 `11-16` 会被当成今年那个已经过去的 11 月 16 日
  - 年龄按农历算：过生日那天长一岁，没到就减 1
  - 两种写法：`birthday` 带年份（`"2001-12-03"`）＝ 只知公历出生日、按农历过；
    只写月日（`"10-19"`）＝ 直接当农历月日（只记得农历生日时用，代价是算不出年龄）
  - 兜底都做了：生日是三十而该月只有廿九 → 退到当月最后一天；生在闰月的人遇到
    没有闰月的年份 → 按同名非闰月过；超出 1900~2099 → 安静退回公历显示，不崩
- ⚖️ **为这件事专门避了一个许可坑**：最省事的做法是 `pip install lunardate`，但它
  在 PyPI 上标 MIT，源码头部却写明 *derived from the GPLv2 `lunar` project* ——
  本项目是 MIT，抄它的表或把它打进 exe 都会污染许可。所以改用 MIT 的
  [borax](https://github.com/kinegratii/borax) **生成数据并内嵌**（数据是天文事实：
  每年正月初一落在哪天、哪个月大哪个月小、哪一年有闰月；编码格式是本项目自己的），
  发布版因此**不增加任何依赖**。表可重现：`python scripts/gen_lunar_table.py`
- ✅ 农历换算实测：`scripts/gen_lunar_table.py --check` 把 **1900-01-31 ~ 2099-12-31
  共 73,019 天**逐日与 borax 比对 + 每个农历日的月长上下界比对，**0 处不符**；
  同时用 lunardate 当第二种裁判交叉验证未来 6 年的生日落点，三实现一致
- 🔬 数据来源：借鉴了开源项目 [pet-reminder](https://github.com/kaiCATs/pet-reminder)
  的"生日 = 每年重复的普通事件 + 提前量"字段设计、[DyberPet](https://github.com/ChaozhongLiu/DyberPet)
  的"陪伴天数按天结算"，以及 [AIRI](https://github.com/moeru-ai/airi) 把生日写进
  人格设定的做法（**只借鉴设计，没有搬运代码** —— DyberPet 是 GPL-3.0）
- ✅ 实测：`pet_profile.py` 31 个用例（含 2/29、跨年、脏数据、今天生日）全过；
  卡片用真实 `PetWindow` 走了一遍右键菜单路径渲染出图，确认排版没截断

- 🐛 顺带修掉两处 **Windows 中文控制台（GBK）** 的坑：
  - `build.py` 是唯一没调 `app_paths.fix_console_encoding()` 的入口，已补上
  - `wizard.py` 自检成功那行的 `✓` 和 `scripts/tts_preview.py` 的 `▶` 是 GBK
    打不出的字符，换成 `[OK]` / `>>`（保护在时不崩，但会显示成 `?`）
- 🐛 修掉档案卡的一处排版 bug：`QLabel` 开了 `wordWrap` 却没钉死宽度时，
  Qt 算不准高度，**空档案的填写指引最后一行会被裁掉**（代码片段少了半行）。
  已改成显式设宽 + `setHeightForWidth(True)`


## v0.10.6 (2026-09-15)

**睡觉改成单段式 + 语音降级能自愈**

- 😴 **睡觉不再做"打哈欠→睡着循环"的过渡**，改为**直接切到睡觉循环**（就像 idle→eat
  那样）。原因：参考了 [DyberPet 的素材规范](https://raw.githubusercontent/ChaozhongLiu/DyberPet/master/docs/art_dev.md)
  和 [DeskPet 的 PET.md](https://raw.githubusercontent.com/2048Nemo/DeskPet/main/PET.md) ——
  这类桌宠**所有状态切换都是硬切**，两段式用 `loops` 配置表达（播一遍 vs 无限循环），
  **没有任何项目在做跨素材的过渡动画**。因为素材是成套做的，取景天生一致，硬切就好看。
  跨素材做溶镜/形变从一开始就在解决一个不存在的问题
- 🔧 运行时对应放宽：**只有资源包里同时存在 `sleep` 和 `sleep_loop` 时**才做"播一遍
  再交接"的两段式处理；只有 `sleep.gif` 时按普通循环播（否则会播一遍就冻在最后一帧）
- 🔊 **语音降级后能自愈**：以前 edge 神经语音连续失败 3 次就"本次会话不再用它"，
  宠物跑久了遇上几次网络抖动就会**永久变成系统 SAPI5 老声音**，而用户只听到
  "声音变难听了"却无从排查。现在降级 5 分钟后会自动重试
- 🔍 新增**语音启动自检**：启动时试合成一句固定的话，把"神经语音就绪/不可用及原因"
  写到日志里。以前 `synth()` 把异常全吞了，降级完全无声无息

## v0.10.5 (2026-09-15)

**睡着后的循环段换成新素材（`绿幕下打哈欠睡觉循环动画 4.mp4`）**

- 🎬 `sleep_loop.gif` 用新素材重做：**52 帧 / 1.44 MB**（旧版 86 帧 / 3.61 MB）。
  新素材质量更好 —— 全程主体占比稳定在 32.2~32.6%、没有硬切、没有水印，
  内部帧间最大 2.13（旧版 3.95），循环接缝也由新加的 `close_loop_seam()` 自动缝好
- 🔗 **交接处做了溶镜**：新素材是**更宽的取景**（主体高 265 vs 原素材的 411），
  直接换上会在"打完哈欠进入循环"那一刻尺寸跳一下（差异 32.04，正常帧间只有 2.67）。
  做法是把 12 帧交叉溶解**烘焙进 `sleep.gif` 的结尾**，而且**最后一帧正好等于新循环的首帧** ——
  运行时切换是**零差异**（实测 0.00）
- 📐 顺带修掉一个 1 像素的不一致：缩放帧尺寸时用的是 `round()`（720×960 → 320×427），
  而早先素材是 `int()` 截断（→ 320×426）。同一个资源包里各状态尺寸必须一致，
  否则切状态时窗口宽度会差 1px。已改回 `int()`

## v0.10.4 (2026-09-15)

**循环动画的"接缝"**

- 🔁 **睡着后的循环段每转一圈会跳一下**：`sleep_loop.gif` 的首尾没闭合
  （末帧→首帧的差异 7.52，是它内部最大帧间差 3.15 的 **2.4 倍**），
  所以每 6.4 秒循环一次都会闪一下。已用交叉溶解把接缝缝合：
  **接缝 7.52 → 0.86**，全片最大帧间差 **7.52 → 3.95**，且前 80 帧与原文件逐像素一致
- 🔧 新增 `video_ops.close_loop_seam()`，并在转换时对**会一直循环的状态**
  （`idle` / `sleep_loop` / `talk`）自动缝接缝，插入帧数按接缝大小自适应
  （目标是每步变化降到 1.5 以下，3~10 帧）
  - `sleep.gif` **不在此列**：它只播一遍然后交接给 `sleep_loop`，首尾本来就不该一样
  - 实测：`sleep_loop` 接缝 2.53（内部最大 4.33）、`idle` 接缝 1.40（内部最大 8.64）
- 顺带排查了你其它动画：`idle` 也永久循环但接缝本来就正常（7.33 < 内部最大 8.72）；
  `eat` 有接缝但投喂只播 2.5 秒、够不到 3.9 秒的接缝，无需处理

## v0.10.3 (2026-09-15)

**三个实测出来的问题**

- 🖱️ **点一下就能触发反应了**（原来必须长按）。根因在 `mouseReleaseEvent` 里
  无条件 `apply_state("idle")` —— 一松手就切回待机，所以点按几乎什么都看不见，
  非得一直按着才行。现在：**点按**让 click 动画自己播一会儿（默认 2.5s，
  可用 `click_hold_ms` 调）再回待机；**拖动**（位移超过 4px）才立刻回待机。
  顺带修掉"手抖 2px 就被当成拖动"的问题
- 💥 **修掉一个连点宠物会冻结程序的死锁**（严重）。`QMediaPlayer` 的
  `stop() -> setSource() -> play()` 反复churn 会让 Qt6 的 FFmpeg 后端死锁 ——
  主线程永久卡在 `stop()` 里，界面直接冻结。用 `faulthandler` 才抓到栈
  （普通看门狗线程因为主线程持着 GIL 打不出日志）。
  实测**连点 40 次左右就能复现**，而且是竞态、时快时慢。
  改为：同一文件重播不碰 `setSource`（也就不需要 `stop`），换文件才 `setSource`
  （它本身就会停掉旧的，显式 `stop` 是多余的）。修复后 **200 轮 × 5 次全过**
- 🎬 **睡觉那段的过渡不再花屏**。原先用的**抖动溶解**数值指标很漂亮，但肉眼是
  一层噪点状的双重曝光（"站着打哈欠"和"躺下"两个姿势按噪声图混在一起）——
  **数值好看不等于好看**。改成**交叉溶解**（电影里的溶镜），重影是柔和的，
  不再是花屏

## v0.10.2 (2026-09-15)

**素材扔进来就能用：抠像不再挑背景色 + 硬切自动处理**

- 🎨 **背景自适应抠像**（`scripts/video_ops.py`）：原来只认绿幕（只看绿色优势度），
  白底、蓝底、任何纯色现在都能抠。做法是从画面四周采样背景色，按颜色距离做软 alpha
- 👕 **不会被同色误伤**：只把**与画面边缘连通**的相似色区域当背景 ——
  否则穿白衣服站在白墙前，衣服会跟着墙一起被抠掉（有测试守着这条）
- 🎬 **硬切自动检测 + 抹平**：AI 视频里常见的"两个镜头硬接在一起"（就是你之前
  遇到的"抽搐一下"）现在会自动检出并用抖动溶解摊平。判据要求是个**单帧尖峰** ——
  "轻戳"这类快速动作连续几帧都高，不会被误当硬切抹掉（实测 34.3/30.0/34.0 正确放过）
- 🧠 **复杂背景自动换 rembg**：判据不是猜，而是**看抠像结果** ——
  边框里"看起来像背景"的像素有没有被抠掉。太差就自动改用 rembg 逐帧抠 + 时域中值抑制抖动
- 🧹 **角落水印**：能自动清掉**与主体分离**的角标（实测把一个「AI生成」水印从
  0.65% 清到 0.00%）。贴着主体的水印**不尝试** —— 我试过按"浅色+低饱和+静止"识别，
  结果在两条干净素材上误报、各吃掉 5% 主体，同时漏掉真水印，所以撤掉了
- ⚡ 你现有四个素材的转换总耗时从 75.6s 降到 23.4s（全部走快路径）
- 向导第④步现在会显示转换进度（读帧、背景判断、抠图方式、硬切情况）

## v0.10.1 (2026-09-15)

**图形化制作向导：不碰命令行也能做一只自己的桌宠**

- 🧙 新增 `wizard.py`（`python wizard.py`，托盘菜单也有「✨ 制作新桌宠…」入口）。
  五个步骤、每步都有预览：选照片（支持拖入）→ 起名字/选模型 → 生成形象
  → 加动作素材（视频拖进来变动画，**顺便自动抽出音效**）→ 完成（改台词/人设、导出 `.pet`、启动）
- ♻️ 向导本身只是界面：抠图、转 GIF、抽音轨、导出全部**复用已有的工具函数**
  （`photo_to_pet` / `add_asset` / `extract_audio` / `pet_pack`），没有第二套逻辑
- ⚙️ 抠图在后台线程跑，界面不卡；进度不确定时用滚动文案让人知道它没死；
  失败会给可操作的建议（换 u2net 模型 / 先跑 download_model.py）
- 🥚 制作期间写在 `pets/.building-<名字>/` **暂存目录**，点完成才挪成正式资源包 ——
  否则目录监视会让宠物在向导做到一半时就冒出来，而且是缺动画的半成品
- 👀 **目录监视**：程序每 5 秒看一眼 `pets/`，出现新的完整资源包（带 `pet.json`）就
  自动加进来并打声招呼。所以向导做完宠物**自己就冒出来了**，导入 `.pet` 之后
  也不用重启（`.pet` 导入本来就是即时生效的）
- 📄 README「从头做一个」改为向导优先，命令行方式保留在下面

## v0.10.0 (2026-09-15)

**资源包能流动了：`.pet` 导入 / 导出**

- 📦 新增 `runtime/pet_pack.py`：把资源包打包成单个 `.pet` 文件（就是 zip），
  以及导入别人的 `.pet`。附命令行 `python runtime/pet_pack.py export|import|check`
- 🖱️ 三种导入方式：托盘菜单选文件、**把 `.pet` 拖到 `PhotoPet.exe` 上**、
  以及可选的**双击 `.pet` 直接安装**（托盘菜单里开启，只写 HKCU，可随时取消）
- 🔄 导入后**不用重启**：直接把新宠物加进正在运行的那一批，托盘菜单同步更新，
  开机自启的宠物列表也跟着带上它
- 🔒 **存档一律不进 `.pet`**：`status.json`（好感度）/ `work.json`（陪伴时长）
  导出时剔除 —— 分享形象不等于把养成记录送人。已用测试断言守住这条
- 🛡️ **导入按不可信输入处理**：zip slip 防护（拒绝 `..` / 绝对路径 / 盘符 / 符号链接）、
  白名单目录（`avatar/`、`animation/`、`sounds/` 之外的东西一律丢弃）、文件数与解压体积上限
- 🐛 过程中修掉一个**自身的安全漏洞**：归一化路径时用了 `lstrip("./")`，
  它会把 `../../evil` 的前导点斜杠整段吃掉 —— 攻击痕迹被抹平、检查反被绕过
  （没造成实际逃逸是因为后面还有一道绝对路径兜底）。现在安全检查看的是**原始**条目名
- 兼容"多包了一层目录"的压缩包（很多人会把文件夹整个压进去），并能正确读出宠物名字

## v0.9.1 (2026-09-15)

**开源首发门面：示例资源包 + 演示动图**

- 🐛 **修掉一个一直都在的形象变形 bug**：窗口尺寸原本用 `QMovie.frameRect()` 推算，
  而它有两个坑 —— 动画还没解码出首帧时返回 `0×0`（于是用了硬编码的 320×240 兜底），
  缩放生效后又返回**缩放后**的尺寸（错误宽高比因此自我固化、再也纠正不回来）。
  结果宠物一直被**横向拉伸 1.77 倍**（213×160，正确应为 120×160）。
  改用加载时通过 `QImageReader` 读到的原生尺寸后恢复正常
- 📦 新增示例资源包 `demo-pet/`（形象经本人授权）：名字「小陪」、打磨过的台词与人设
- 🎯 **全新 clone 即玩**：首次启动时如果 `pets/` 里一只宠物都没有，会把 `demo-pet/`
  自动装成 `pets/demo/`。放在 `pets/` **之外**是刻意的 —— `pets/` 被整目录忽略，
  而 git 不允许重新包含"父目录已被忽略"的文件；复制而不是直接用，则保证存档落在被忽略的 `pets/` 里
- 🗜️ 示例包体积减半：动画从 15.9MB 压到 7.5MB（宽度 320→240、颜色 255→64）。
  240 宽对应动画高 319px，而运行时最大显示高度是 288px —— **任何缩放下都不会被放大**，等于无损
- 🖼️ 新增 `scripts/make_demo_gif.py`：用真实素材合成 README 顶部的演示动图，无需录屏
- 📄 README 增加演示横幅与「30 秒试玩」入口
- 新增 `scripts/make_demo_pack.py`：从你自己的资源包生成可发布的示例包（自动剔除存档与 `.bak`）
- 🤖 **GitHub Actions 自动出包**（`.github/workflows/build.yml`）：
  打 `v*` 标签自动构建 Windows 包并发布到 Releases，用户点一下就能下载；
  也可以在 Actions 页面手动触发（不勾发布就只上传为构建产物，便于发布前先验）。
  `build.py --release` 会把 exe、`_internal`、示例资源包和 `使用说明.txt` 一起打成 zip ——
  CI 就是调它，所以本地跑一遍等于提前验证 CI

## v0.9.0 (2026-09-14)

**工作陪伴 + 说话动画 + 打包 exe**

- 🪑 **工作陪伴**：宠物醒着就在统计"今天陪了你多久"，`pets/<名字>/work.json` 存一份。
  口径是"宠物醒着 **且** 你确实在用电脑"（`GetLastInputInfo`）—— 中途去开会/吃饭的时间不算，
  否则"陪了你 8 小时"会虚高。跨天自动归零当天、保留总计，还能看到陪伴天数
- 💤 **点睡觉 = 今天收工**：睡觉不再是单纯的动画，而是"我停止工作了"的仪式 ——
  收工时会报一句"今天陪了你 3 小时 7 分，收工啦～ 好好休息"
- ⏰ **劝休息升级成有选择的对话**：连续用电脑到点，宠物先出声劝，再弹一个
  **不抢键盘焦点**的小窗（`WA_ShowWithoutActivating`，不影响你打字），
  两个按钮「好，一起睡」「再干一会儿」；40 秒没理它 = 再干一会儿。
  话术逐次升级，**第 3 次起会摆出陪伴时长**："我都陪你工作 3 小时 17 分了，你也需要休息呀，放下手中的工作吧"
- 🗣️ **说话时嘴动**：放 `animation/talk.gif` 即可，说话期间自动切过去、说完切回。
  两个细节：说话时**保持窗口尺寸不变**（否则尺寸不一致会让窗口跳一下）；
  结束判定用「令牌 + 计时器」主控 + `EndOfMedia` 提前收尾（语音会排队，只靠 `EndOfMedia` 会把口型永久卡住）。
  缺素材/关开关则静默降级
- 配置新增：`work_log_enabled` / `rest_snooze_minutes` / `rest_prompt_seconds` / `talk_animation`
- 宠物 tooltip 与右键菜单会显示"今天陪了你 X"，不再和番茄钟抢 tooltip

**打包成 exe（`build.py`）+ 双击即启动**

- 🖱️ `python build.py --install` 一键装好并在**桌面创建带图标的快捷方式**，双击即启动，
  不用再碰命令行。装到项目根目录而非 `%LOCALAPPDATA%`，让 exe 与源码模式
  **共用同一份 `pets/` 与存档**（免得好感度/陪伴时长分裂成两份）
- 🎨 新增 `scripts/make_icon.py` 生成图标：默认画对称爪印，也可以用
  `--image 你的图.png` 直接拿宠物形象做图标（会自动裁掉透明边距并加圆角底）；
  一次生成 `icon.png`（托盘）+ 多尺寸 `icon.ico`（exe 与快捷方式）
- exe 现在带自己的图标（`--icon`），不再是 PyInstaller 的默认图标

- 📦 `python build.py` 产出可分发文件夹，不装 Python 也能双击运行；默认 onedir（启动快）
- 🔧 新增 `runtime/app_paths.py` 统一路径解析，修掉打包后**三个会静默失效的路径**：
  `ai_config.json`、`pets/`、托盘图标原本都指向 PyInstaller 的临时解压目录（退出即删）。
  现在区分「打进 exe 的只读资源」与「用户数据」两类路径
- 💾 用户数据「便携优先、APPDATA 兜底」：解压即用的绿色版存档跟着资源包走；
  装到 `Program Files` 这类只读位置时自动回落 `%APPDATA%/PhotoPet/`，
  并把自带资源包复制过去 —— 顺带修掉"写不进存档还静默吞掉异常"
- 🔒 单实例锁（`QLockFile`）：开机自启 + 手动双击不会起两批桌宠；
  重复启动的提示框 6 秒后自动关闭，不会一直挂在那儿等你去点确定
- 🎯 排除 `rembg`/`onnxruntime`/`numpy`/`scipy`（生成器专用），体积从 1GB+ 降到 ~140MB
- 打包后没资源包时的提示不再教用户敲 `python generator/...`（exe 用户看不懂）
- 实测验证：便携路径、`status.json`/`work.json` 落盘、Qt 多媒体后端（ffmpeg 解码 mp3）、
  劝休息小窗正常弹出、单实例拦截、`%APPDATA%` 回落

## v0.8.2 (2026-09-14)

**状态音效上线：从动画视频里抽出音轨**

- 🎵 `sounds/` 之前一直是空的，所以点击/投喂/睡觉**全都没有声音**（代码是好的，缺素材）。
  新增 `scripts/extract_audio.py`：从你生成的动画视频里抽音轨、统一响度、按状态装进 `sounds/`。
  实测你 `素材库/` 的视频都带音轨，直接抽出了 sleep / sleep_loop / click / eat 四个音效
- 😴 **两段式睡觉声音**：点睡觉 → 打哈欠声（裁到与入睡动画等长，避免被截断）
  → 自动接睡着后的循环动画 + 呼吸声
- 🔇 `apply_state(..., sound=False)`：**随机小动作、缩放、穿透重绘不再出声**
  （否则加了音效之后，随机小动作会每 5 分钟响一次，破坏"安静陪伴"）
- 安静的待机动画音轨（只有底噪）会被自动识别并跳过，不会装一个"沙沙声"进去

## v0.8.1 (2026-09-14)

**安静陪伴：默认不再定时说话**

- 🤫 关掉定时闲聊（原来每 15 秒冒一句台词），改为**只在互动和久坐提醒时开口**
- 🪑 新增**久坐提醒**：用 `GetLastInputInfo` 检测键鼠活动，累计"连续在用电脑"的时长，
  满 45 分钟提醒起来动动、看看远处；中途离开超过 60 秒（倒水/开会）会自动重新计时，
  不会出现"人不在还催你休息"；提醒后重新计一轮，不会连环催
- 🎲 随机小动作改为**只动不出声**，且不再打断睡觉状态（原来是 5 分钟一次动作+台词）
- 🍖 饿了的乞食加了冷却（默认 10 分钟最多一次，原来每分钟都念）
- ⚙️ 以上全部可在 `ai_config.json` 配置：`chatter_seconds` / `random_action_minutes` /
  `hungry_nag_minutes` / `rest_reminder_*`；想找回原来的热闹感，把 `chatter_seconds` 设成 15 即可

## v0.8.0 (2026-09-14)

**语音重做：从系统老引擎换成神经语音**

- 🔊 新增 edge-tts 神经语音后端（免费、无需 API key），音质相比系统 SAPI5 的
  Microsoft Huihui（2010 年的共振峰合成）是代际差别
- 可选 **12 种音色**，含东北话、陕西话、粤语、台湾腔；`scripts/tts_preview.py --all` 可挨个试听
- 💾 **合成缓存**：按「音色+语速+文本」哈希存到 `~/.photopet/tts_cache/`，
  同一句台词第二次说是读本地文件 —— 零延迟且断网可用（台词池重复率高，命中率很高）
- 🛟 **自动降级**：断网/音色名写错时，这一句立刻改用系统语音顶上（不会静默无声），
  连续失败 3 次则本次会话不再尝试；菜单里会显示当前在用什么声音
- ⚙️ 配置化：`ai_config.json` 新增 `tts_engine`(auto/edge/sapi) / `tts_voice` / `tts_rate`，
  换声音不用改代码
- 播放走独立的 QMediaPlayer（与状态音效分声道，说话时自动压掉音效），
  合成在工作线程、播放经 Qt 信号切回主线程

## v0.7.1 (2026-09-14)

健壮性修复：多个已宣传的功能此前实际是坏的，本次全部修好并用运行测试验证。

**修复（影响可用性）**
- 系统托盘从未真正启用：`setContextMenu` / `show` 等四行被误写进 `_quit()`，导致托盘图标不显示、右键菜单为空，`_quit()` 还会抛 `NameError`。现已恢复正常（显示/隐藏、全部现身、大小、穿透、退出）
- 托盘图标空白：`runtime/icon.png` 不存在且 `*.png` 被 gitignore 排除。改为 `icon.png` 优先、缺失时用 QPainter 画爪印兜底
- 「语音开关」形同虚设：`PetTTS.enabled` 同时表示"引擎可用"和"用户开关"，`speak()` 只判断前者，装过 pyttsx3 就会一直念。拆成 `engine_ready` 与 `on` 两个标志，并真正读取 `ai_config.json` 的 `tts_enabled`
- 缩放功能整体消失：`self.zoom` 只有两处 `= 1.0`，滚轮已移除而托盘「大小」菜单从未实现。现已补上 60%~180% 六档
- 位置/大小不记忆：唯一可用的退出路径绕过了保存逻辑。改为统一挂在 `app.aboutToQuit` 上，任何退出方式都会存档
- 依赖清单缺失：`requirements.txt` 没写 `PyQt6`/`pillow`/`rembg`，照 README 安装必崩；反而把可选的 `openai`/`pyttsx3` 列为必需。已按必需/可选重新整理

**安全**
- `.gitignore` 补上 `ai_config.json`（含 API key）与 `素材库/`（含个人形象原始视频）

**其他改进**
- 鼠标穿透加托盘开关：穿透后宠物自身点不到，原本只能杀进程才能脱困（提示文案也改成指向托盘）
- 开机自启动带上 `--pet` 参数：原本开机只会去加载默认资源包，用户自己的桌宠不会出现
- 不带 `--pet` 时自动加载 `pets/` 下全部资源包（原先默认的 `pets/demo` 并不存在）
- AI 回复改用 Qt 信号切回主线程：此前在 worker 线程直接操作 QLabel/QTimer，可能随机崩溃
- `pet.json` 的 `dialogues` 终于生效（此前该字段从未被读取，资源包作者写台词无效）
- 鼠标穿透/全屏隐藏的状态处理更严谨；资源包缺形象图时显示占位图并明确提示，不再变成 0 宽空窗口
- 生成器输出从 160×160 提到 512×512，并为旋转留出余量避免裁切边角
- 清理死代码（`_reg_values`、`main()` 末尾无用的 `import ctypes`、重复的分隔线）与过时文档

## v0.7.0 (2026-09-14)

- 🎵 语音包：资源包新增 `sounds/` 目录，按状态命名（`click.mp3` / `eat.mp3` / `sleep.mp3` …），状态切换时自动播放，支持 mp3/wav/ogg
- 🎞️ 状态动作动画扩充：`animation/` 支持 `eat.gif`（投喂）、`sleep.gif` + `sleep_loop.gif`（入睡过渡播一遍后无缝切入睡着循环）
- 素材安装向导 `scripts/add_asset.py` 与 `添加素材.bat`：把视频/图片/音频拖上去即可自动转换、命名、归位
- 配套文档 [素材制作指南.md](素材制作指南.md)：用豆包/即梦生成动作动画与语音包的完整流程

## v0.6.0 (2026-09-14)

- 👻 鼠标穿透：右键菜单开关，穿透时点击直接落到下层窗口（Win32 WS_EX_TRANSPARENT 扩展样式实现，比 Qt 窗口标志可靠、不闪窗；非 Windows 自动禁用）
- 💾 配置保存：退出时自动记住每只宠物的位置和大小，下次启动原地恢复（Qt 官方 QSettings 方案）
- 🎭 AI 驱动动作：AI 回复命中关键词自动切换动作/表情（参考 DesktopFriends），映射表可通过 pet.json 的 `ai_action_map` 自定义

## v0.5.0 (2026-09-14)

- 💬 AI 对话：OpenAI 兼容接口（默认 DeepSeek，可换任意 base_url），多轮上下文，人设来自 pet.json 的 `persona` 字段；填入 `ai_config.json` 的 api_key 后右键菜单自动出现"和它聊天 (AI)"
- 🔊 离线语音 (TTS)：pyttsx3 本地引擎，右键"语音开关"手动开启，自动过滤颜文字，后台线程播放不卡 UI
- 未配置 api_key / 未安装 pyttsx3 时自动降级，不影响其他功能

## v0.4.0 (2026-09-14)

- 🍅 番茄钟：右键菜单开启/停止，默认 25 分钟专注 / 5 分钟休息（参考开源项目 TomatoClock 的 QTimer 状态机）；专注完成触发庆祝气泡 + 好感度 +10，与养成系统联动；剩余时间显示在鼠标悬停提示中
- 🎲 随机小动作：每 5 分钟随机触发动画与台词（参考竞品"随机模式"盲盒设计）
- 🎮 全屏自动隐藏：检测到前台全屏程序（游戏/演示）时自动隐藏，退出后恢复（Windows 用 GetForegroundWindow 矩形比对，社区标准做法；非 Windows 平台自动禁用）

## v0.3.0 (2026-09-14)

- 养成系统上线（设计参考开源项目 DyberPet 的饱食/心情/好感模型）：
  - 饱食度、心情随时间衰减，每分钟更新，数值驱动台词（饿了乞食、开心卖萌）
  - 右键"投喂"回复饱食度，点击抚摸攒心情与好感度
  - 好感度等级 Lv.1~∞，升级触发庆祝气泡
  - JSON 持久化存档（`status.json`），关闭后数值保留，离线衰减封顶 8 小时
- 右键菜单顶部实时显示 好感/饱食/心情 数值

## v0.2.1 (2026-09-14)

- 抠图模型可切换：默认高质量 `bria-rmbg`（发丝/毛发边缘最佳），低配电脑可用 `--model u2net`（快 3~5 倍）
- 新增国内镜像加速脚本 `scripts/download_model.py`：模型自动走 ghproxy 镜像下载到 rembg 缓存目录，解决 GitHub 直连过慢问题

## v0.2.0 (2026-09-14)

- GIF/序列动画支持：资源包新增 `animation/` 目录，状态可映射到同名 GIF（参考 Desktop_Pet_LLM 的 QMovie 方案）
- 滚轮缩放：0.6x ~ 1.8x，实时按新尺寸重绘（参考 OpenDesktopPet 的缩放设计）
- 系统托盘：常驻托盘图标，支持显示/隐藏每只桌宠、全部现身、退出（参考 Qt 官方 systray 示例，`setQuitOnLastWindowClosed(False)`）
- 开机自启动：Windows HKCU Run 注册表，无需管理员权限，源码运行用 pythonw 避免黑窗
- 多桌宠：`--pet` 支持多个资源包目录，一次启动多只共存

## v0.1.0 (2026-09-14)

- 首个版本发布
- 桌宠运行时：透明悬浮窗、拖拽、点击/双击互动、气泡对话、右键菜单
- 照片生成器：rembg 抠图，生成 idle / click / sleep 三态资源包
- 预留 photo2cartoon Q 版卡通化接入接口

## 计划中 (Roadmap)

- [ ] photo2cartoon 一键安装脚本（把照片变成保留五官的 Q 版形象）
- [ ] 多表情 AI 批量生成（一次产出全套动作）
- [ ] 桌宠资源包分享站 / 一键导入
- [ ] 全局热键（穿透模式的键盘开关、快捷键投喂）
- [ ] macOS / Linux 的托盘与图标适配打磨

> 已完成的历史计划（GIF 帧动画、喂食/心情/成长系统）见上方各版本条目。
